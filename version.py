"""
version.py
----------
Single source of truth for the application version number, so it stays
consistent across the About window, installer, and README instead of
drifting out of sync.
"""

APP_VERSION = "1.2.0"
